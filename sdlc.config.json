{
  "_comment": "Autonomy dial per stage. mode is one of: 'approve' (agent produces its artifact, pipeline pauses for your y/n before saving; on reject it re-runs with your feedback), 'auto' (agent runs, pipeline continues without asking), or 'manual' (you do the stage yourself). mode governs the human pause for the text-producing stages (parse, spec, review). The gated stages run a deterministic orchestrator-side check regardless of mode: implement is gated by the product's syntax check, and qa by the full E2E suite + acceptance-criteria (AC-N) coverage. So qa is effectively a hard gate that mode cannot disable — 'auto' there only means 'do not pause for a human before the gate runs'. Deploy (not yet a stage) will likewise be a hard, non-configurable gate.",
  "_comment_enabled": "Each stage also takes an 'enabled' flag (defaults to true when omitted). Set 'enabled': false to skip that stage entirely — no agent runs, no gate runs, and the dashboard shows the stage as 'skipped'. Skipping is literal, not smart: if you disable a stage whose artifact a later stage consumes (e.g. spec, which implement/review/test/qa all read via specs/<id>.md), that later stage fails when the artifact is missing. Disabling qa removes the authoritative ship gate — the pipeline will finish at the last enabled stage with nothing verifying acceptance criteria.",
  "project": {
    "productPath": "../Homebase",
    "knowledgePath": "../OKF"
  },
  "stages": {
    "parse":     { "enabled": true, "mode": "approve" },
    "spec":      { "enabled": true, "mode": "approve" },
    "implement": { "enabled": true, "mode": "auto", "maxTurns": 30 },
    "review":    { "enabled": true, "mode": "auto" },
    "test":      { "enabled": false, "mode": "auto" },
    "qa":        { "enabled": true, "mode": "auto" }
  },
  "learning": { "enabled": true },
  "_comment_mcp": "Optional MCP wiring. 'configPath' points at an MCP-format file in THIS repo (default 'mcp.json') whose ${ENV} placeholders resolve from a gitignored .env (copy .env.example). 'stages' maps a stage to the MCP tool patterns it may call — a stage not listed gets no MCP, so removing this block restores the no-MCP behavior. Servers load with --strict-mcp-config (reproducible; ignores desktop/user/claude.ai connectors). Missing ${ENV} credentials fail fast at startup.",
  "mcp": {
    "configPath": "mcp.json",
    "stages": {
      "spec": ["mcp__MongoDB__*"]
    }
  },
  "_comment_fetch": "Optional pre-fetch of external debugging context for the `npm run sdlc fetch <uploadJobId>` command. Deterministic (not agent/MCP-driven): pulls the upload job's MongoDB document and its matching Sentry events into context/<id>/context.{md,json}. Requires SENTRY_AUTH_TOKEN (Sentry User Auth Token / Internal Integration with scopes event:read, project:read, org:read) and MONGODB_URI in this repo's .env (see .env.example). sentry.queryTemplate substitutes {id} with the job id — confirmed live to be the Sentry tag 'uploadJobId:{id}' on upload-invoice events. sentry.statsPeriod for the issues endpoint accepts ONLY '', '24h', or '14d' (a Sentry API limitation — '90d' returns HTTP 400). Remove this block to disable the command.",
  "fetch": {
    "sentry": {
      "org": "monto-e1c13a9a2",
      "regionUrl": "https://us.sentry.io",
      "projects": ["upload-invoice"],
      "queryTemplate": "uploadJobId:{id}",
      "statsPeriod": "14d",
      "maxIssues": 25,
      "maxEventsPerIssue": 5
    },
    "mongo": { "database": "MontoProd", "collection": "upload_jobs" }
  }
}
